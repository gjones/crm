%h1
  Preferences

= form_for @user do |f|
  - if @user.errors.any?
    #error_explanation
      %h2
        = pluralize(@user.errors.count, "error")
        prohibited this user from being saved:
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    %label
      = f.label :theme
      = f.select :theme, [:dark, :light, :white]
      %br/
      %br/
      = f.submit "Update Theme"

